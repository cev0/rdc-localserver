name: Last Shelter Engagement Rewards

on:
  push:
    branches: [server-v2-scalable]
    paths:
      - 'last_shelter_engagement_reward_reference.js'
      - 'last_shelter_engagement_reward_reference_testi.js'
      - 'server.js'
      - 'server_unit_regressiya_testi.js'
      - '.github/workflows/last-shelter-engagement-rewards.yml'
  pull_request:
    paths:
      - 'last_shelter_engagement_reward_reference.js'
      - 'last_shelter_engagement_reward_reference_testi.js'
      - 'server.js'
      - 'server_unit_regressiya_testi.js'
      - '.github/workflows/last-shelter-engagement-rewards.yml'

jobs:
  regression:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: node last_shelter_engagement_reward_reference_testi.js
